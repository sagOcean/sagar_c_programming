#include <stdio.h>
#include<string.h>

int main() 

{
    // strlen, strcpy , strcat, strcmp

    /*
    strlen:- prints the length of the given string excluding the null character
        char name[]="sagar";
        printf("%d",strlen(name));
    */

    /*
    strcpy :- copies the value of one string and puts it on the next string

    char name1[6]= "hello";
    char name2[6];

    strcpy(name2,name1); // strcpy(copy,original)
    printf("name1:%s\n",name1);
    printf("copied name:%s\n",name2);
    */ 

    /*
    strcat:- concatenates(joins) two strings    (lets try three)//doesnt support 3 strings

    char a1[7]="hello ";
    char a2[7] = "world ";
    

    
    printf("%s",strcat(a1,a2));
    */

    //strcmp

    


    
    return 0;
}